<Window x:Class="ServerUDP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServerUDP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Запустити сервер" Click="StartServer_Click" Margin="0,0,10,0"/>
            <Button Content="Зупинити сервер" Click="StopServer_Click"/>
        </StackPanel>

        <ListBox x:Name="LogListBox" Grid.Row="1" FontFamily="Consolas" FontSize="14"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem Content="{Binding ServerStatus}" />
        </StatusBar>
    </Grid>
</Window>
