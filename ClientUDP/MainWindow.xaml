<Window x:Class="ClientUDP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="ServerIpTextBox" Width="150" Margin="0,0,10,0" Text="127.0.0.1" />
            <TextBox x:Name="ServerPortTextBox" Width="80" Margin="0,0,10,0" Text="150" />
            <Button Content="Підключитися" Click="ConnectButton_Click" />
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="InputTextBox" Width="250" Margin="0,0,10,0" FontSize="14" FontFamily="Consolas"
                     Text="Введіть назву комплектуючої"/>
            <Button Content="Отримати ціну" Width="120" Click="SendButton_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="2" x:Name="ResponseTextBlock" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="16"/>
    </Grid>
</Window>
